InitList(&L)        构造一个空的线性表
Length(L)           求表长
LocateElem(L,e)     按照值查找操作，在表L中查找具有给定关键字值的元素
GetElem(L,i)        按位查找，获取表L中第i个位置的值
ListInsert(&L,i,e)  插入操作，在表L中第i个位置上插入指定元素e
ListDelete(&L,i,&e) 删除操作，在表L中第i个位置上删除元素e，并用e返回删除元素的值
PrintList(L)        输出操作，按照前后顺序顺序输出L所有元素值
Empty(L)            判空操作，若L为空表，返回true，否则返回false
DestroyList(&L)     销毁链表，释放线性表L所占用的空间